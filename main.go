package main

import (
	"fmt"
)

func main() {

	fmt.Println("Hello World")

	/*multipleIndicators := []indicators.Indicators{indicators.NewEMA(2, "close"), indicators.NewSMA(2, "close")}
	newData := utils.NewOHLCV("close", 3.5)
	res1 := multipleIndicators[0].Calculate(newData)
	res1 = multipleIndicators[0].Calculate(newData)
	res1 = multipleIndicators[0].Calculate(newData)
	res1 = multipleIndicators[0].Calculate(newData)
	res1 = multipleIndicators[0].Calculate(newData)
	res1 = multipleIndicators[0].Calculate(newData)
	res2 := multipleIndicators[1].Calculate(newData)
	res2 = multipleIndicators[1].Calculate(newData)
	res2 = multipleIndicators[1].Calculate(newData)
	res2 = multipleIndicators[1].Calculate(newData)
	res2 = multipleIndicators[1].Calculate(newData)

	fmt.Println(res1)
	fmt.Println(res2)*/
}
